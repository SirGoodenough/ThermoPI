HOST: 10.10.100.10      # Broker IP Address Here
PORT: 18833             # This needs to be a number, usually 1883 as the MQTT port for tcp access
USER: HassSmartbed      # MQTT Username Here
PWD: s74aqvDn8bgJzRG    # MQTT User Password Here'
TEMP: HUDU/temperature1 # MQTT temperature topic here, usually node/temperature where node is this device name
HUMI: HUDU/humidity1    # MQTT humidity topic here, usually node/humidity where node is this device name
LWT: HUDU/lwt           # MQTT last will topic here, usually node/lwt where node is this device name
PIN: 4                  # The GPIO pin used to connect the device
LOOP: 285.0150          # This needs to be a number.  Time in seconds for each loop.  Too fast breaks things so I suggest ~ 300 seconds IE 5 minutes.
